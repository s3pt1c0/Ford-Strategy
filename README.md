# Ford PCM / ECM Decoder Web App

Web app to upload Ford AsBuilt `.ab` files, decode VIN information, inspect PCM/ECM (`7E0`), and review other detected modules.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
